1.早期系统：一个库，进程直接使用物理内存，没有任何抽象
2.多道程序和时分共享
3.地址空间：每个进程都有自己的地址空间，地址空间是一个线性的地址序列，每个地址对应一个字节

地址空间
一个进程的地址空间包含运行程序所需要的所有内存状态
假设只有三部分：代码、栈、堆
代码：顶部，静态且无需新的空间
堆：顶部，动态分配的内存空间，由malloc()等函数分配，向下增长
栈：底部，动态分配的内存空间，由函数调用和返回时分配和释放，向上增长

目标
1.透明
2.效率
3.保护